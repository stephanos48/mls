@model List<mls.ViewModels.BomViewModel>

@{
    ViewBag.Title = "Build Ability";
}

<h2>Build Ability</h2>

<p>
    @Html.ActionLink("Go Back", "Index", "WorkOrders", null, null) | @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("BuildAbility", "BomLevel1", FormMethod.Get))
{
    @Html.TextBox("search") <input type="submit" name="submit" value="search" />
}
<br/>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>
                BomNo
            </th>
            <th>
                UnitNo
            </th>
            <th>
                DetailPn
            </th>
            <th>
                Description
            </th>
            <th>
                Qoh
            </th>
            <th>
                QtyPer
            </th>
            <th>
                BuildAbility
            </th>
        </tr>
    </thead>
    <tbody>
        @*@if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="3" style="color:red">
                        No Matches Found.  Please request assembly be added to BOM.
                    </td>
                </tr>
            }
            else
            {*@
        @foreach (var item in Model)
            {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.BomNo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UnitNo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DetailPn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Qoh)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QtyPer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BuildAbility)
                </td>

            </tr>
        }

    </tbody>
</table>